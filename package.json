{
  "name": "termux-web-app",
  "version": "1.0.0",
  "description": "Termux interface for automation downloaders",
  "main": "server.js",
  "scripts": {
    "build": "pip install yt-dlp || pip3 install yt-dlp",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
